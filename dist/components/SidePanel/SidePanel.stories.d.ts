import { default as React } from 'react';
declare const meta: {
    component: ({ open, onClose }: import('./SidePanel').SidePanelProps) => React.JSX.Element;
};
export default meta;
export declare const HandlingClose: () => React.JSX.Element;
