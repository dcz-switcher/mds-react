import { default as React } from 'react';
declare const meta: {
    component: ({ titleIcon, children, open, onClose, size, title, hideCloseButton, footer }: import('./SidePanel').SidePanelProps) => React.JSX.Element;
};
export default meta;
export declare const HandlingClose: () => React.JSX.Element;
