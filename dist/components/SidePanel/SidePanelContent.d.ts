import { default as React, ReactNode } from 'react';
export type SidePanelContentProps = {
    children?: ReactNode;
    className?: string;
};
declare const SidePanelContent: ({ children, className, }: SidePanelContentProps) => React.JSX.Element;
export default SidePanelContent;
