import { default as React } from 'react';
export type SidePanelHeaderProps = {
    title?: string;
    icon?: string;
};
declare const SidePanelHeader: ({ title, icon }: SidePanelHeaderProps) => React.JSX.Element;
export default SidePanelHeader;
