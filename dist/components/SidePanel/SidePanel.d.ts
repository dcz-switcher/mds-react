import { default as React } from 'react';
export type SidePanelProps = {
    open?: boolean;
    onClose?: any;
};
declare const SidePanel: ({ open, onClose }: SidePanelProps) => React.JSX.Element;
export default SidePanel;
