import { default as React, ReactNode } from 'react';
export type SidePanelFooterProps = {
    children?: ReactNode;
};
declare const SidePanelFooter: ({ children }: SidePanelFooterProps) => React.JSX.Element;
export default SidePanelFooter;
