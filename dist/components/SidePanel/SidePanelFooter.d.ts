import { default as React, ReactNode } from 'react';
export type SidePanelFooterProps = {
    children?: ReactNode;
    className?: string;
};
declare const SidePanelFooter: ({ children, className, }: SidePanelFooterProps) => React.JSX.Element;
export default SidePanelFooter;
