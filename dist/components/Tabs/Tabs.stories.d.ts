import { default as React } from 'react';
declare const meta: {
    component: ({ reverse, children, className, }: import('./Tabs').TabsProps) => React.JSX.Element;
};
export default meta;
export declare const Default: () => React.JSX.Element;
