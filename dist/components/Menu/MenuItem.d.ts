import { default as React, ReactNode } from 'react';
export type MenuItemProps = {
    children?: ReactNode;
};
declare const MenuItem: ({ children, }: MenuItemProps) => React.JSX.Element;
export default MenuItem;
