import { default as React } from 'react';
export type HeaderProps = {
    sticky?: boolean;
    logoHref?: string;
};
declare const Header: ({ sticky, logoHref, }: HeaderProps) => React.JSX.Element;
export default Header;
