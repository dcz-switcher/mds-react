import { default as React, ReactNode } from 'react';
export type HeaderNavProps = {
    children?: ReactNode;
    className?: string;
};
declare const HeaderNav: ({ children, className, }: HeaderNavProps) => React.JSX.Element;
export default HeaderNav;
