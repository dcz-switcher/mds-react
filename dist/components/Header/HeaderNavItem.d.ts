import { default as React } from 'react';
export type HeaderNavItemProps = {
    label?: string;
    className?: string;
};
declare const HeaderNavItem: ({ label, className, }: HeaderNavItemProps) => React.JSX.Element;
export default HeaderNavItem;
