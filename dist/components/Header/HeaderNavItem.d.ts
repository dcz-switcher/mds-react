import { default as React } from 'react';
export type HeaderNavItemProps = {
    label?: string;
    className?: string;
    href?: string;
};
declare const HeaderNavItem: ({ label, className, href, }: HeaderNavItemProps) => React.JSX.Element;
export default HeaderNavItem;
