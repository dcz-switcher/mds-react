import { default as React, ReactNode } from 'react';
export type CalloutActionProps = {
    children?: ReactNode;
};
declare const CalloutAction: ({ children }: CalloutActionProps) => React.JSX.Element;
export default CalloutAction;
