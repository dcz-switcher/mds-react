import { default as React, ReactNode } from 'react';
export type CarouselSlideProps = {
    children?: ReactNode;
    className?: string;
};
declare const CarouselSlide: ({ children, className }: CarouselSlideProps) => React.JSX.Element;
export default CarouselSlide;
