import { default as React, ReactNode } from 'react';
export type AccordionProps = {
    children?: ReactNode;
    className?: string;
};
declare const Accordion: ({ children, className, }: AccordionProps) => React.JSX.Element;
export default Accordion;
