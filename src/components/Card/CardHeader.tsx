import { ReactNode } from "react";

export type CardHeaderProps = {
    children?: ReactNode;
}

const CardHeader = ({
    children,
}:CardHeaderProps) => {
    return (
        children
    )
};

export default CardHeader;